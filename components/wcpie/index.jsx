'use strict';

import React from 'react';
import PropTypes from 'prop-types';
import classNames from 'classnames';
import './index.scss';

const prefix = 'aisc-wcpie';

export default class WCpie extends React.Component {
  constructor(props) {
    super(props);
  }

  render() {
    return ()
  }
}
