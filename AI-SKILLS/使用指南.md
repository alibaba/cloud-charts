# CloudCharts AI 技能套件 - 使用指南

## 🎯 本文档目标

本文档提供从零开始使用 AI 技能套件完成 CloudCharts 重构的完整指导。

**适合人群**: 
- 项目负责人 (了解整体规划)
- 开发工程师 (执行具体任务)
- AI 辅助编程爱好者

**阅读时间**: 15分钟  
**实践时间**: 1-2小时 (完成第一个里程碑)

---

## 📦 前置准备

### 1. 环境要求

```bash
# 检查 Node.js 版本
node --version  # 需要 >= 20.6.0

# 检查 npm 版本
npm --version   # 需要 >= 9.0.0

# 检查 Git
git --version   # 需要 >= 2.0.0
```

### 2. 安装 OpenSkills

```bash
# 全局安装 OpenSkills CLI
npm install -g openskills

# 验证安装
openskills --version  # 应该显示版本号

# 查看帮助
openskills --help
```

### 3. 准备 CloudCharts 项目

```bash
# 进入项目目录
cd /path/to/cloud-charts

# 创建备份分支
git checkout -b backup/pre-refactor
git add .
git commit -m "backup: pre-refactor state"
git push origin backup/pre-refactor

# 切换到重构分支
git checkout -b refactor/ai-driven
```

---

## 🚀 快速开始 (5分钟)

### 步骤 1: 安装技能套件

```bash
# 进入项目根目录
cd /path/to/cloud-charts

# 复制技能套件
# (假设你已经将 AI-SKILLS 文件夹放在项目根目录)
ls AI-SKILLS/
# 应该看到: SKILL.md  README.md  scripts/  使用指南.md

# 安装到 Claude (可选)
mkdir -p ~/.claude/skills
cp -r AI-SKILLS/.claude/skills/* ~/.claude/skills/

# 或者使用 OpenSkills 安装
openskills install ./AI-SKILLS
```

### 步骤 2: 验证安装

```bash
# 查看可用技能
openskills list

# 应该看到:
# ✓ cloud-charts-reconstructor
# ✓ cloud-charts-dev
# ✓ cloud-charts-testing
# ✓ cloud-charts-docs

# 查看技能详情
openskills info cloud-charts-reconstructor
```

### 步骤 3: 执行第一个任务

```bash
# 仅执行构建工具迁移 (快速验证)
openskills run cloud-charts-reconstructor --phase=build-tools --dry-run

# 如果一切正常，执行实际迁移
openskills run cloud-charts-reconstructor --phase=build-tools
```

**预期输出**:
```
✅ vite.config.ts 已创建
✅ scripts/compile-themes.js 已创建
✅ package.json 已更新
✅ tsconfig.json 已更新
✅ test/setup.ts 已创建

📊 执行完成
   耗时: 2.5s
   修改文件: 5
   下一步: npm install && npm run build
```

---

## 📖 详细使用场景

### 场景 1: 完整自动化重构

**适合**: 首次重构，希望一次性完成

```bash
# 1. 准备环境
cd /path/to/cloud-charts
git checkout -b refactor/ai-complete

# 2. 执行完整重构
openskills run cloud-charts-reconstructor --phase=all --verbose

# 3. 等待完成 (预计 1-2 小时)
# AI 会自动:
#   - 分析项目
#   - 执行所有 7 个阶段
#   - 运行测试
#   - 生成文档
#   - 输出报告

# 4. 查看报告
cat reports/REFACTOR-REPORT.md

# 5. 验证结果
npm run build  # 应该快速成功
npm test       # 应该全部通过
npm run storybook  # 应该正常启动
```

**监控进度**:
```bash
# 在另一个终端查看日志
tail -f logs/refactor.log

# 或查看当前阶段
cat .refactor-status.json
```

### 场景 2: 分阶段执行

**适合**: 需要控制节奏，逐步验证

```bash
# 阶段 1: 构建工具 (今天)
openskills run cloud-charts-reconstructor --phase=build-tools
# 验证: npm run build

# 阶段 2: React 18 (明天)
openskills run cloud-charts-reconstructor --phase=react18
# 验证: npm test

# 阶段 3: G2 5.x (后天)
openskills run cloud-charts-reconstructor --phase=g2-5x
# 验证: 手动测试图表

# 阶段 4: Hooks (第4-10天)
openskills run cloud-charts-reconstructor --phase=hooks
# 验证: 代码审查

# 阶段 5: AI 体系 (第11天)
openskills run cloud-charts-reconstructor --phase=ai-system
# 验证: AI 能否生成代码

# 阶段 6: 测试 (第12天)
openskills run cloud-charts-testing --phase=all
# 验证: 覆盖率报告

# 阶段 7: 文档 (第13天)
openskills run cloud-charts-docs --phase=all
# 验证: 文档完整性
```

### 场景 3: 特定任务执行

**适合**: 只需要完成某个具体任务

#### 3.1 添加新图表组件

```bash
# 使用开发专家技能
openskills run cloud-charts-dev --task="add-component" --component="boxplot"

# AI 会:
# 1. 分析需求
# 2. 创建 src/Wboxplot/
# 3. 生成代码
# 4. 添加测试
# 5. 更新导出
# 6. 生成文档
```

#### 3.2 修复 Bug

```bash
# 使用开发专家技能
openskills run cloud-charts-dev --task="fix-bug" --bug-id="issue-123"

# AI 会:
# 1. 复现问题
# 2. 定位代码
# 3. 应用修复
# 4. 运行测试
# 5. 验证修复
```

#### 3.3 性能优化

```bash
# 使用测试专家技能
openskills run cloud-charts-testing --task="performance-test"

# AI 会:
# 1. 运行基准测试
# 2. 识别瓶颈
# 3. 应用优化
# 4. 验证改进
# 5. 生成报告
```

#### 3.4 生成文档

```bash
# 使用文档专家技能
openskills run cloud-charts-docs --task="generate-api-docs"

# AI 会:
# 1. 扫描代码
# 2. 提取类型
# 3. 生成文档
# 4. 添加示例
# 5. 输出 Markdown
```

### 场景 4: 与 Claude 集成

**适合**: 使用 Claude Code 或 Claude.ai

#### 4.1 Claude Code

```bash
# 在 Claude Code 中
/plugin marketplace add anthropics/skills

# 然后安装本技能
/plugin install cloud-charts-reconstructor

# 直接对话
用户: "使用 cloud-charts-reconstructor 完成重构"
Claude: 自动执行...
```

#### 4.2 Claude.ai

```bash
# 上传技能文件
1. 打开 Claude.ai
2. 上传 AI-SKILLS/SKILL.md
3. 说: "请学习这个技能"
4. 然后: "使用这个技能重构 CloudCharts"
```

### 场景 5: 自定义技能

**适合**: 需要扩展或修改技能

```bash
# 1. 编辑主技能
vim AI-SKILLS/SKILL.md

# 2. 添加新脚本
vim AI-SKILLS/scripts/custom-task.js

# 3. 测试修改
openskills test cloud-charts-reconstructor

# 4. 更新版本
# 修改 SKILL.md 中的 version 字段

# 5. 重新安装
openskills install ./AI-SKILLS --force
```

---

## 🔧 高级配置

### 1. 自定义执行参数

创建配置文件 `.openskillsrc`:

```json
{
  "cloud-charts-reconstructor": {
    "defaultPhase": "build-tools",
    "verbose": true,
    "timeout": 3600,
    "retry": 3,
    "reportFormat": "markdown",
    "outputDir": "./reports"
  },
  "cloud-charts-dev": {
    "testFirst": true,
    "generateDocs": true,
    "codeStyle": "typescript"
  }
}
```

### 2. 环境变量配置

创建 `.env` 文件:

```bash
# 项目信息
CLOUD_CHARTS_VERSION=2.0.0
REFACTOR_START_DATE=2026-01-09

# AI 行为模式
AI_MODE=autonomous  # autonomous | assisted | manual
AI_TEMPERATURE=0.3  # 创造性程度

# 测试配置
TEST_COVERAGE_THRESHOLD=80
PERFORMANCE_BUDGET=100ms

# 输出配置
VERBOSE_LOG=true
SAVE_INTERMEDIATE=true
```

### 3. 钩子脚本

创建 `.openskills/hooks/`:

```bash
# pre-refactor.sh (重构前)
#!/bin/bash
echo "准备重构..."
npm run lint
git status

# post-refactor.sh (重构后)
#!/bin/bash
echo "重构完成，正在验证..."
npm run build
npm test
echo "✅ 验证通过"
```

---

## 📊 监控与调试

### 1. 查看执行进度

```bash
# 实时日志
tail -f logs/refactor.log

# 阶段状态
cat .refactor-status.json

# 修改文件列表
cat .refactor-changes.txt
```

### 2. 调试模式

```bash
# 详细输出
openskills run cloud-charts-reconstructor --phase=build-tools --verbose

# 模拟执行 (不实际修改)
openskills run cloud-charts-reconstructor --phase=build-tools --dry-run

# 单步执行
openskills run cloud-charts-reconstructor --phase=build-tools --step-by-step
```

### 3. 性能监控

```bash
# 监控构建时间
time npm run build

# 监控测试时间
time npm test

# 监控内存使用
/usr/bin/time -v npm run build
```

---

## 🎯 验证清单

### 阶段 1: 构建工具迁移 ✅

- [ ] `vite.config.ts` 存在且正确
- [ ] `npm run build` 在 10 秒内完成
- [ ] 生成 `dist/index.es.js`
- [ ] 生成 `dist/index.cjs.js`
- [ ] 生成 `dist/index.umd.js`
- [ ] 生成 `dist/index.d.ts`
- [ ] 包体积 < 500KB
- [ ] 无构建警告

### 阶段 2: React 18 ✅

- [ ] `package.json` 中 React 版本为 ^18.2.0
- [ ] `npm ls react` 显示 18.x
- [ ] 所有测试通过
- [ ] 无弃用警告
- [ ] Storybook 正常工作

### 阶段 3: G2 5.x ✅

- [ ] `npm ls @antv/g2` 显示 5.x
- [ ] 所有图表渲染正常
- [ ] 配置项功能一致
- [ ] 主题切换正常
- [ ] 事件系统正常
- [ ] 性能无明显下降

### 阶段 4: Hooks ✅

- [ ] 存在 `useChart` Hook
- [ ] 函数组件版本可用
- [ ] API 完全兼容
- [ ] 代码更简洁
- [ ] 性能提升

### 阶段 5: AI 体系 ✅

- [ ] `.claude/skills/` 目录存在
- [ ] 所有 SKILL.md 文件完整
- [ ] 类型定义完善
- [ ] 文档生成正常
- [ ] AI 可以生成代码

### 阶段 6: 测试 ✅

- [ ] 单元测试覆盖率 > 80%
- [ ] 集成测试通过
- [ ] 性能基准达标
- [ ] 无回归问题

### 阶段 7: 文档 ✅

- [ ] API 参考完整
- [ ] 迁移指南清晰
- [ ] 使用示例丰富
- [ ] 技术文档详细

---

## ❓ 常见问题

### Q1: 技能安装失败

**错误**: `Cannot find module 'openskills'`

**解决**:
```bash
npm install -g openskills
# 或
npx openskills install ./AI-SKILLS
```

### Q2: 构建脚本权限不足

**错误**: `EACCES: permission denied`

**解决**:
```bash
chmod +x AI-SKILLS/scripts/*.js
# 或
node AI-SKILLS/scripts/migrate-build.js
```

### Q3: G2 迁移后图表不显示

**原因**: API 变化未完全适配

**解决**:
```bash
# 1. 查看错误
npm run dev 2>&1 | grep error

# 2. 手动检查组件
# 对比 G2 5.x 文档
# https://g2.antv.vision/docs/api

# 3. 使用调试模式
openskills run cloud-charts-reconstructor --phase=g2-5x --verbose
```

### Q4: 测试覆盖率不足

**解决**:
```bash
# 1. 查看覆盖率报告
npm run test:coverage

# 2. 识别未覆盖的代码
# 3. 使用测试专家补充
openskills run cloud-charts-testing --task="add-missing-tests"
```

### Q5: 文档生成失败

**解决**:
```bash
# 1. 检查类型定义
npx tsc --noEmit

# 2. 手动运行 TypeDoc
npm run docs

# 3. 查看错误日志
cat docs/typedoc-errors.log
```

---

## 💡 最佳实践

### 1. 始终备份
```bash
# 每次重构前
git tag backup-$(date +%Y%m%d-%H%M%S)
```

### 2. 小步快跑
```bash
# 不要一次性执行所有阶段
# 分阶段执行，每阶段后验证
openskills run cloud-charts-reconstructor --phase=build-tools
# 验证
openskills run cloud-charts-reconstructor --phase=react18
# 验证
```

### 3. 充分测试
```bash
# 每个阶段后都运行测试
openskills run cloud-charts-testing --phase=current
```

### 4. 及时文档
```bash
# 每个阶段后生成文档
openskills run cloud-charts-docs --phase=current
```

### 5. 代码审查
```bash
# 使用 AI 辅助审查
openskills run cloud-charts-dev --task="code-review"
```

---

## 📞 获取帮助

### 1. 查看日志
```bash
# 完整日志
cat logs/refactor.log

# 错误日志
cat logs/error.log

# 调试信息
cat logs/debug.log
```

### 2. 社区支持
- GitHub Issues: 提交 bug 或建议
- 文档: 查看 AI-SKILLS/README.md
- 示例: 查看 AI-SKILLS/使用指南.md

### 3. 联系维护
```bash
# 报告问题
openskills report-issue cloud-charts-reconstructor

# 请求帮助
openskills request-help cloud-charts-reconstructor
```

---

## 🎉 总结

您现在已经掌握了使用 AI 技能套件完成 CloudCharts 重构的全部知识！

### 下一步行动

1. **立即开始**: 执行 `openskills run cloud-charts-reconstructor --phase=build-tools`
2. **深入学习**: 阅读 AI-SKILLS/SKILL.md 了解所有能力
3. **实践应用**: 尝试不同的任务和场景
4. **分享经验**: 将您的使用经验反馈给我们

### 预期成果

完成重构后，您将获得:
- ✅ 现代化的技术栈 (React 18 + G2 5.x + Vite)
- ✅ 完整的测试套件 (>80% 覆盖率)
- ✅ 详细的文档体系
- ✅ AI 编程能力
- ✅ 5-10 倍的开发效率提升

---

**祝您重构顺利！** 🚀

**文档版本**: 1.0  
**最后更新**: 2026-01-09

